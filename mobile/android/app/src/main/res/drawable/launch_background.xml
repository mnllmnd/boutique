<?xml version="1.0" encoding="utf-8"?>
<!-- ✅ Native Android splash screen (shown before Flutter loads) -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fond avec gradient sombre -->
    <item android:drawable="@color/dark_background" />
    
    <!-- Loader simple pendant que Flutter initialise -->
    <item>
        <shape xmlns:android="http://schemas.android.com/apk/res/android">
            <solid android:color="@color/dark_background" />
        </shape>
    </item>
</layer-list>
