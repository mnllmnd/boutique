// Mobile-only PDF download handler stub
// On mobile, we'll use share_plus instead

Future<void> downloadPdfOnWeb(List<int> pdfBytes, String fileName) async {
  // This function is not used on mobile
  // Mobile uses share_plus in the main code instead
}
